/**
 * Mirrors the Prisma `CvAnalysisStatus` enum.
 * Framework-agnostic — no NestJS or Prisma imports.
 */
export enum CvAnalysisStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
}
